import EditQuestionCell from 'src/components/EditQuestionCell'

const EditQuestionPage = ({ roomId, id }) => {
  return <EditQuestionCell id={id} roomId={roomId} />
}

export default EditQuestionPage
